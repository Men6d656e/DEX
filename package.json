{
  "name": "dex-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Decentralized Learning & Trading Dashboard (Demo DEX) — A full-stack Web3 educational dashboard",
  "license": "MIT",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "test": "npm run test --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
